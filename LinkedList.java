public class LinkedList {
    
}


/* 
Linked List 
 -> singly ll (normal int data; node next;) 
 -> doubly ll (int data, node pre, next;)
 -> circular ll (last node -> next to head)
 -> doublely circular ll (last node -> head and vice versa)

 declaration of linkedlist node ->
 public class Node {
  int val;
  Node next;
  Node prev; // for doublely linkedlist

  public Node(int data){
     this.val = data;
     this.next = null;
     this.prev = null;  //for doubly linkedlist
  }
 }

 creation of node with Node class 
 Node node = new Node();
*/
