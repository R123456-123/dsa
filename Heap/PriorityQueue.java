package Heap;

public class PriorityQueue {
    
}

// PriorityQueue<type> heap = new PriorityQueue<>();
    // PriorityQueue<type> heap = new PriorityQueue<>((a,b) -> (a - b)); -> minHeap
    // PriorityQueue<type> heap = new PriorityQueue<>((a,b) -> (b - a)); -> maxHeap
    // PriorityQueue<type> heap = new PriorityQueue<>((a,b) -> (a[0] - b[0])); -> map, list with 2 values in i
    // PriorityQueue<type> heap = new PriorityQueue<>((a,b) -> Integer.compare(a,b));  -> overflow control
